import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Mesozoic-Era - Explore the World of Dinosaurs",
  description:
    "Your interactive journey into the Mesozoic Era. Discover 13 magnificent dinosaurs with detailed information, 3D visualizations, and engaging features.",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
